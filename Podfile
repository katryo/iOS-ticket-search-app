# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'EventTicket' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EventTicket
  # pod 'Alamofire', '~> 4.7'
  pod "SwiftSpinner"
  pod "EasyToast"
  #pod 'Alamofire-SwiftyJSON', '>= 3.0.0'
  pod 'McPicker'
  pod 'Kingfisher'
  #pod 'Cosmos', '~> 17.0'

  target 'EventTicketTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EventTicketUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
